# Changelog

## v1.0.0 (2026-07-29)

首个正式版本。

### 功能
- 🖌️ 画笔工具：自由绘制，贝塞尔平滑笔迹，可选颜色与粗细
- 🖍 荧光笔工具：半透明 + multiply 混合，划重点不遮字
- 🏷 标注元素：文字标注（所见即所得输入）、箭头、矩形框
- 🧽 橡皮擦：基于 `destination-out` 合成模式真实擦除
- ↩️ 撤销 / 重做：操作栈实现，支持 `Ctrl+Z` / `Ctrl+Y`
- 🗑 清空画布：一键清空（清空操作本身也可撤销）
- 💾 保存导出：整页 + 标注导出 PNG（按需加载 html2canvas，失败自动降级为标注层）
- 📐 图层管理：画布浮于内容之上；退出标注模式后 `pointer-events: none` 完全穿透，不影响原页面交互
- ✋ 可拖动工具栏：右上角手柄拖动，自动限制在视口内
- 📱 响应式：适配 DPR / 窗口缩放无损重绘；移动端工具栏切换为底部横排，支持触屏

### 交付形态
- `webpaint.js`：单文件零依赖插件（UMD），可直接 `<script>` 引入或 `require`
- `extension/`：Chrome / Edge MV3 浏览器扩展，在任意网页点图标即可标注，截图用浏览器原生 `captureVisibleTab`，不联网
- `demo/`：可运行的演示页面

### 说明
- 插件核心唯一来源为根目录 `webpaint.js`；扩展副本通过 `node tools/sync.js` 同步
- 图标由 `node tools/gen-icons.js` 生成（纯 Node，无依赖）
